pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Days_without_bad_habits"
include(":androidApp")
include(":shared")